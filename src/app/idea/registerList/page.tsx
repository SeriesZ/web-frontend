import RegisterList from "@/components/idea/RegisterList";
import React from "react";

type Props = {};

const IdeaRegisterListPage = (props: Props) => {
  return <RegisterList />;
};

export default IdeaRegisterListPage;
